dist: trusty
language: node_js
node_js:
  - 10.6.0
jobs:
  include:
  - stage: test
    cache:
      directories:
      - node_modules
    install:
    - yarn
    script:
    # - yarn test

    # Use Travis to build and push containers to Docker
  - stage: deploy
    sudo: required
    services:
    - docker

    script:
    # Deploy to EC2
    - bash deploy.sh

notifications:
  email: false
